version: '3.8'

services:
  icalarchive:
    build: .
    ports:
      - "8000:8000"  # Calendar feeds
      - "8001:8001"  # Web UI
    volumes:
      - ./data:/data
    restart: unless-stopped
    environment:
      - TZ=UTC
